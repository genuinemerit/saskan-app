name: saskan
channels:
  - defaults
dependencies:
  - pip
  - mypy
  - flake8
  - isort
  - arrow
  - pathlib
  - redis
  - requests
  - tzlocal
  - wxpython

# And:
# sudo apt install libcanberra-gtk-module libcanberra-gtk3-module
