name: saskan
channels:
  - conda-forge
  - defaults
dependencies:
# Other dependencies are installed using pip:
#  - pygame
  - python=3.10.6
  - pip
  - mypy
  - flake8
  - isort
  - arrow
  - pathlib
  - requests
  - tzlocal
  - sqlite
  - numpy
  - pandas
  - twisted
  - tornado
  - music21
  - sympy
  - matplotlib
  - nltk
  - Levenshtein
  - networkx

# May be added later:
#  - odfpy

# Required at OS level:
# - netstat (via net-tools installed with apt-get)

# Suggested for trying to do graphviz with networkx
# - graphviz gsfonts (via apt-get)

# 2021-10-10  11:00
# Path: saskan.yml

# pygame was not available in the conda-forge channel
# Have to install it using pip.

# conda env list
# conda env remove -n saskan
# conda env create -f ./saskan.yml
# conda activate saskan
# conda deactivate
