# Remote Procedure Call (RPC)

Call a procedure on another process/machine as if it were local; the transport and serialization are hidden behind a request/response abstraction.

- Server: exposes procedures over the network and returns results.
