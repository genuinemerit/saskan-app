# Model–View–ViewModel (MVVM)

UI pattern where the ViewModel exposes the data and commands the View needs, without UI or domain logic leaking across layers.

- Model: core domain/game state.
- View: UI (e.g., Qt widgets, PyGame canvas).
- ViewModel: mediator exposing UI‑friendly data/events and commands.
