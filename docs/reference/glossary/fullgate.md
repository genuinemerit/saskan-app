# Full Gate

The complete set of checks (lint, typecheck, tests, etc.) that code must pass before merging. “Run the full gate” = run these checks locally before pushing/PR.

- Typical checks: lint/format, type-check, unit tests, and CI wiring.
- In this repo: `make check` runs the full gate locally.

